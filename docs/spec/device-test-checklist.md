---
title: 실기기 검증 체크리스트
kind: snapshot
last_verified: 2026-09-04
---

# 실기기 검증 체크리스트

현재 코드(HEAD) 기준으로 실기기에서 확인할 항목과 결과. `README.md` 수동 검증 매트릭스가 "무엇을 어떻게 확인하는가"의 정본이고, 이 문서는 **진행 상태와 코드 사전 추적 결과**만 소유한다. 항목이 전부 통과·기록되면 결과를 `README.md`에 옮기고 이 문서는 삭제 후보다.

기기: iPhone 15 Pro (iPhone16,1), iOS 26.6. 결과 표기: ✅ 통과 / ❌ 실패 / ⬜ 미확인. 실패는 어떻게 보였는지를 비고에 적고 `LLM_REPORT.md` 사례 후보로 삼는다.

## 1. 기본 동작

2026-09-04 영상 판독 회차: 사용자가 촬영한 실기기 화면 녹화 5편(각 약 20초)을 1fps 프레임으로 추출해 판정. 결과의 "영상"은 그 근거.

| 항목 | 실기기 | 비고 |
| --- | --- | --- |
| 카메라 프리뷰 표시 | ✅ | 영상 전편 |
| 스캔 시작 → 상태 배지, pts·cells 카운트 증가 | ✅ | 대기 → 스캔 중 전환, 23,998 → 1,483,584 pts 누적 관찰 |
| 미니맵 실시간 채워짐 | ✅ | 스캔 진행에 따라 격자 증가 |
| 미니맵 색상이 실제 공간과 대응 (흰 벽 = 흰 셀, 갈색 바닥 = 갈색 셀) | ✅ | 전체화면에서 흰 침대보 = 흰 셀 뭉치, 어두운 가구·바닥 구분 확인 |
| 오버레이 창 배경 균일, 어두운 사각형 없음, 마커 중앙 고정 | ✅ | 전 프레임에서 균일 |
| 벽 라인 구분, 바닥이 통째로 밝게 칠해지지 않음 | ⬜ | 전체화면 촬영 필요 |
| 알려진 길이 벽의 미니맵 스케일 | ⬜ | 영상으로 측정 불가 |
| 카메라 뜬 뒤 화면 멈춤 없음 (mesh 예열 중) | ⬜ | 프레임 추출 판독으론 프레임 드랍 판정 불가 |
| 스캔 시작 전 mesh 안 보임 | ✅ | 대기 상태 전 프레임에서 mesh 없음 |
| 첫 시작 시 mesh 빠른 표시 | ✅ | 콜드 런치 직후 즉시 시작한 경우 약 2초 — 그 공백은 "주변 인식 중…" 배지가 커버 (#34) |
| 일시정지·재개·초기화 동작 | ✅ | 영상 3편에서 반복 확인 |
| 미니맵 탭 전체화면 전환 | ✅ | 전체화면 전환·닫기 버튼 동작 확인 |

## 2. 시나리오 검증

"코드 추적" 열은 실기기 전에 소스에서 시나리오를 따라간 결과 (`LLM_REPORT.md` 사례 12). **성립**은 코드상 그렇게 동작해야 한다는 뜻이지 검증이 아니다.

### 최근 커밋 재확인

| # | 시나리오 | 확인 방법 | 코드 추적 | 실기기 | 비고 |
| --- | --- | --- | --- | --- | --- |
| 1 | 초기화 → 시작 | mesh 표시가 이전(약 1초)보다 빨라졌는가 | 성립 — 구성 교체 2회 → 1회 (`8b7afa4`). 남는 지연은 ARKit mesh 재생성이라 코드로 판단 불가 | ✅ | 초기화 후 즉시 시작(최악 케이스) 실측: mesh 표시까지 약 2~2.5초, 그 공백 전체를 #34 배지가 커버 — UX 목적 달성으로 판정 |
| 2 | 초기화 직후 | 트래킹 재초기화 중 mesh가 미리 보이지 않는가 | 성립 — `ready` 상태에서 `showMesh = false` | ✅ | 초기화 직후 대기 상태에서 mesh 없음, "트래킹 초기화 중…" 배지 표시 후 소멸 |
| 3 | 일시정지 → 이동 → 재개 | 궤적선이 끊기지 않고 이어지는가 | 성립 — `hasStarted` 게이트 (`f638896`) | ✅ | 오버레이 미니맵에서 파란 궤적선 연속 확인. 전체화면 정밀 확인은 #10에서 |
| 4 | 일시정지 → 이동 → 재개 | 새 영역이 실제 거리만큼 떨어진 자리에 그려지는가 | 성립 — 세션·트래킹 유지, 월드 좌표 누적 | ✅ | 일시정지 채 옆방 이동 → 재개 → 미니맵에 기존 영역과 분리된 자리에 새 격자. 거리 정량 확인은 안 함 |
| 5 | 스캔 시작 전 이동 | 궤적이 안 남는가 | 성립 — `hasStarted = false` | ✅ | 대기 중 이동 후 시작 — 시작 직후 미니맵에 궤적선 없음 |
| 6 | 초기화 | 궤적이 비는가 | 성립 — `reset`에서 `trajectory = []`, `hasStarted = false` | ✅ | 초기화 직후 미니맵에 마커만 |
| 32 | 일시정지 | mesh 와이어프레임이 사라지는가 | 성립 — `showMesh = (state == .scanning)` | ✅ | 영상 3편에서 일시정지 즉시 mesh 완전 소멸, pts 동결(1,458,277 / 680,080) |
| 33 | 재개 | mesh가 즉시(로딩 없이) 다시 보이는가. 일시정지 중 걸어간 영역 mesh도 함께 보임(허용) | 성립 — 표시는 `debugOptions` 토글, 재구성은 계속 돎 | ✅ | 1fps 프레임 간격 안에 mesh 전체 복귀 — 로딩 없음 |
| 34 | 초기화 → 스캔 시작 | "주변 인식 중…" 배지가 뜨고 첫 mesh 표시와 함께 사라지는가 | 성립 — `meshReady` 이벤트(첫 `ARMeshAnchor` didAdd)로 해제. 고정 타이머 아님 | ✅ | 두 트리거 모두 확인 — 콜드 시작: 배지(5,634 pts) → 1초 뒤 mesh와 소멸. 초기화 → 시작: 배지(43,558 pts, mesh 없음) → 1초 뒤 mesh 표시와 동시 소멸 |

### 미니맵

| # | 시나리오 | 확인 방법 | 코드 추적 | 실기기 | 비고 |
| --- | --- | --- | --- | --- | --- |
| 7 | 6m 창 | 걸어 나갔다 돌아오면 이전 영역이 다시 나타나는가. 6m가 적당한가 | 성립 — 격자 유지, View 변환만 카메라 추종. 카메라 중앙 정합은 수식으로 확인 | ⬜ | 반경은 `ContentView`의 `visibleRadius` |
| 8 | 제자리 회전 | 맵은 고정, 삼각형만 도는가. 오른쪽으로 돌면 삼각형도 시계방향인가 | 성립 — +x(동)를 볼 때 heading π/2, 화면 y-down에서 양의 회전 = 시계방향 | ⬜ | |
| 9 | 시야 부채꼴 | 실제 카메라 방향과 맞는가 | 성립 — 짧은 호(60°) 방향 | ⬜ | |
| 10 | 궤적선 | 걸은 경로대로 그려지는가 | 성립 — 마커와 같은 좌표 변환 | ✅ | 전체화면에서 궤적선이 이동 경로 형태로 격자 위에 표시. 장거리 경로 정밀 대조는 안 함 |
| 11 | 전체화면 | 관측 영역 전체가 보이고 궤적·마커가 격자와 같은 위치인가 | 성립 — `visibleRadius = nil`, 항등 변환 | ✅ | 전체화면에서 관측 격자 전체 + 궤적·마커 정합 확인 |
| 12 | 초기화 후 미니맵 | 격자가 비고 마커만 남는가 | 성립 — "스캔 대기 중" 텍스트는 뜨지 않는 것이 정상 (초기화 직후 프레임이 바로 스냅샷을 만든다) | ✅ | 초기화 직후 격자 비움·마커만, "스캔 대기 중" 텍스트 없음 |
| 13 | 일시정지 중 이동 | 마커는 움직이고 pts 카운트는 멈추는가 | 성립 — 스냅샷은 매 프레임 발행, `totalPoints` 불변 | ✅ | 영상 3편: pts 동결 상태로 마커·궤적만 갱신 |

### 바닥·천장 필터

| # | 시나리오 | 확인 방법 | 코드 추적 | 실기기 | 비고 |
| --- | --- | --- | --- | --- | --- |
| 14 | 바닥을 향해 스캔 | 벽 셀(원색)이 안 생기는가 | 성립 — 기기 높이 ≥ 1.2m이면 바닥 y < −0.9 | ⬜ | 시작 높이 의존 |
| 15 | 천장을 향해 스캔 | 아무것도 안 그려지는가 | 성립 — 천장 y > +0.7이면 버림. 천장이 낮거나 조명이 매달려 있으면 벽 밴드에 걸릴 수 있음 | ✅ | 순수 천장(조명 포함) 스캔 구간에서 cells 3,658 → 3,660 거의 불변 — 천장 점 폐기 실증 |
| 16 | 앉아서 또는 책상 위에서 시작 | 벽이 바닥으로, 바닥이 벽으로 분류되는가 | **한계 확정** — 기기 0.6m면 바닥 y = −0.6이 벽 밴드에 들어가 바닥 전체가 원색 | ⬜ | `README.md` 제약 절. 깨지는 정도를 보고 ARPlaneAnchor 방식 우선순위 결정 |
| 17 | 가구 | 책상·의자가 벽처럼 그려지는가 | **의도된 동작** — 상판 0.75m는 벽 밴드 | ✅ | 전체화면에서 침대·책상·가구가 원색 셀로 표시, 바닥·미관측과 구분돼 가독성 양호 |

### 트래킹·예외

| # | 시나리오 | 확인 방법 | 코드 추적 | 실기기 | 비고 |
| --- | --- | --- | --- | --- | --- |
| 18 | 빠르게 흔듦 | "기기를 천천히 움직여 주세요" 배지, 앱 지속 | 성립 — `excessiveMotion` 메시지, 죽는 경로 없음 | ⬜ | 세게 흔든 영상에서 배지 미포착 — 트래킹이 normal 유지돼 limited 자체가 안 걸림(앱은 지속·모션블러 중에도 정상). 더 극단적 흔들기 또는 배지 순간 포착 필요 |
| 19 | 흔드는 동안 | pts 카운트가 멈추고 안정되면 재개. 궤적에 스파이크 없음 | 성립 — 누적·궤적 모두 `normal` 게이트 (`adff017`에서 궤적 게이트 복원) | ⬜ | limited(특징 부족) 중 pts 정지·재개는 #20에서 실증. 궤적 스파이크 여부는 limited 직후 전체화면 컷 필요 |
| 20 | 어두운 곳·흰 벽만 | "주변이 어둡거나 특징이 부족합니다" 배지 | 성립 — `insufficientFeatures` 메시지 | ✅ | 어두운 방에서 배지 표시 + pts 정지(372,443 → 372,812) 확인. 밝은 곳 복귀 후 배지 해제·누적 재개(→ 504,479) |
| 21 | 백그라운드 → 복귀 | "세션이 중단되었습니다" 배지가 떴다 사라짐 → "위치 재인식 중…" → 이전 미니맵이 같은 자리에 유지 | **코드로 판단 불가** — `sessionShouldAttemptRelocalization = true` 명시 (`adff017`). 실제 재로컬라이즈 성공 여부는 ARKit·환경 의존 | ✅ | 완전 시퀀스 확인: 복귀 → "세션이 중단되었습니다" → "트래킹 초기화 중…" → "위치 재인식 중…" → 약 4초 내 해제·누적 재개(601,438 → 702,534 pts). 나가기 전(482,013 pts)·복귀 후(820,110 pts) 전체화면 격자 같은 자리 |
| 22 | 카메라 권한 거부 | 안내 화면 + 설정 열기. 허용 후 복귀하면 정상 | 성립 — 권한 변경 시 iOS가 앱을 종료하므로 재실행 경로. 오류 후 "다시 시도"는 ARView 재생성 (`adff017`에서 예열 플래그 해제) | ⬜ | 앱 삭제 후 재설치로 확인 |

### 드리프트

| # | 시나리오 | 확인 방법 | 코드 추적 | 실기기 | 비고 |
| --- | --- | --- | --- | --- | --- |
| 23 | 복도 왕복 또는 방 한 바퀴 | 시작점으로 왔을 때 벽이 두 겹으로 그려지는가 | **코드로 판단 불가** — 보정 없음 (`DESIGN.md` 12절) | ⬜ | 정도를 기록. 가산점 후보 5 판단 근거 |
| 24 | "위치 재인식 중…" 이후 | 격자가 어긋나는가 | **코드로 판단 불가** — 재로컬라이즈 후 앵커 미세 조정은 격자에 반영하지 않음 | ✅ | 재인식 후 mesh가 실물(침대)에 정합, 전체화면 격자에 이중 셀 없음, 이어 스캔 정상 누적 |

### 성능 (signpost 코드 추가 후 별도 진행)

| # | 항목 | 확인 방법 | 실기기 | 비고 |
| --- | --- | --- | --- | --- |
| 25 | 3분 이상 연속 스캔 | 강제 종료·메모리 경고 없음 (Instruments Allocations) | ⬜ | |
| 26 | UI fps | 스캔 중 ≥ 30 (Xcode FPS 게이지) | ⬜ | |
| 27 | 콜백 처리 시간 | p50 / p95 (os_signpost, Points of Interest) | ⬜ | 색 샘플링 추가 후 비용 확인 |
| 28 | 프레임당 처리 점 수 | DEBUG 로그 | ⬜ | 설계상 ≤ 3,072 |
| 29 | 전후 비교 | stride 1·interval 0으로 같은 공간 재측정 | ⬜ | 가산점 "성능 최적화 근거" +2 |

### 기록

| # | 항목 | 실기기 | 비고 |
| --- | --- | --- | --- |
| 30 | iPhone 15 Pro, iOS 26.6을 `README.md` 상단 표에 기입 | ⬜ | |
| 31 | 결과를 `기기 × iOS × 시나리오 × 결과`로 `README.md` 매트릭스에 반영 | ⬜ | |

## 3. 우선순위

1. 18, 19, 20 (흔들기·어두움 배지) — 게이트 복원 확인.
2. 7, 8, 9 — 6m 창·제자리 회전·시야 부채꼴 (미니맵 기하 촬영분).
3. 14, 16 — 바닥 방향·앉은 시작 높이 밴드 판단.
4. 나머지 순서대로 (23 드리프트, 22 권한, 25~29 성능, 30~31 기록).
